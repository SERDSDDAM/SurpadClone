#!/bin/bash

# ===============================================
# إعادة تشغيل سريع للتطبيق فقط
# نظام بنّاء اليمن - البيئة المحلية
# ===============================================

set -e

echo "🔄 إعادة تشغيل التطبيق..."

# إعادة تشغيل التطبيق فقط (أسرع من إعادة تشغيل كل شيء)
if docker-compose -f docker-compose.local.yml restart app; then
    echo "✅ تم إعادة تشغيل التطبيق بنجاح"
    echo "🌐 التطبيق متاح على: http://localhost:3000"
else
    echo "❌ فشل في إعادة التشغيل"
    echo "💡 جرب: ./start-local.sh"
fi